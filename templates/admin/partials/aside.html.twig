<nav id="sidebar" class="sidebar js-sidebar">
    <div class="sidebar-content js-simplebar">

        {# ---- Logo SchoolPrepar ---- #}
        <a class="sidebar-brand" href="{{ path('admin_dashboard') }}">
            <span class="align-middle">🎓 SchoolPrepar</span>
        </a>

        <ul class="sidebar-nav">

            {# ---- Section principale ---- #}
            <li class="sidebar-header">Navigation</li>

            <li class="sidebar-item {% if app.request.pathInfo starts with '/admin/dashboard' %}active{% endif %}">
                <a class="sidebar-link" href="{{ path('admin_dashboard') }}">
                    <i class="align-middle" data-feather="home"></i>
                    <span class="align-middle">Dashboard</span>
                </a>
            </li>

            {# ---- Section Orientation ---- #}
            <li class="sidebar-header">Orientation</li>

            <li class="sidebar-item {% if app.request.pathInfo starts with '/admin/filiere' %}active{% endif %}">
                <a class="sidebar-link" href="{{ path('admin_filiere') }}">
                    <i class="align-middle" data-feather="book-open"></i>
                    <span class="align-middle">Gestion filières</span>
                </a>
            </li>

            <li class="sidebar-item {% if app.request.pathInfo starts with '/admin/etablissement' %}active{% endif %}">
                <a class="sidebar-link" href="{{ path('admin_etablissement') }}">
                    <i class="align-middle" data-feather="map-pin"></i>
                    <span class="align-middle">Gestion etablissements</span>
                </a>
            </li>

        </ul>

        {# ---- Bloc bas de sidebar ---- #}
        <div class="sidebar-cta">
            <div class="sidebar-cta-content">
                <strong class="d-inline-block mb-2">SchoolPrepar</strong>
                <div class="mb-3 text-sm">
                    Aidons les jeunes à faire les bons choix pour leur avenir.
                </div>
            </div>
        </div>

    </div>
</nav>
