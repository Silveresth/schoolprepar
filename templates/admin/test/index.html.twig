{%  extends 'admin/base.html.twig' %}
{% block title %}Tests{% endblock %}
{% block content %}
<div class="container-fluid p-0">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="h3">📋 Tests</h1>
        <a href="{{ path('admin_test_new') }}" class="btn btn-primary">➕ Nouveau</a>
    </div>

    {%  for message in app.flashes('success') %}
        <div class="alert alert-success alert-dismissible fade show">{{ message }}<button type="button" class="btn-close" data-bs-dismiss="alert"></button></div>
    {%  endfor %}

    <div class="card">
        <div class="card-body p-0">
            <table class="table table-hover mb-0">
                <thead class="table-dark">
                    <tr>
                        <th>#</th>
                        <th>Titre</th>
                        <th>Date</th>
                        <th>Durée</th>
                        <th>Questions</th>
                        <th class="text-center">Actions</th>
                    </tr>
                </thead>
                <tbody>
                    {%  for item in items %}
                    <tr>
                        <td>{{ item.id }}</td>
                        <td>{{ item.titre }}</td>
                        <td>{{ item.date|date('d/m/Y') }}</td>
                        <td>{{ item.duree ? item.duree ~ ' min' : 'N/A' }}</td>
                        <td>{{ item.questions|length }}</td>
                        <td class="text-center">
<a href="{{ path('admin_test_show', {'id': item.id}) }}" class="btn btn-sm btn-info">👁️</a>
                                <a href="{{ path('admin_test_edit', {'id': item.id}) }}" class="btn btn-sm btn-warning">✏️</a>
                                <form method="post" action="{{ path('admin_test_delete', {'id': item.id}) }}" class="d-inline"
                                  onsubmit="return confirm('Confirmer la suppression ?')">
                                <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ item.id) }}">
                                <button type="submit" class="btn btn-sm btn-danger">🗑️</button>
                            </form>
                        </td>
                    </tr>
                    {%  else %}
                    <tr><td colspan="6" class="text-center text-muted py-4">Aucun enregistrement.</td></tr>
                    {%  endfor %}
                </tbody>
            </table>
        </div>
    </div>
</div>
{% endblock %}
