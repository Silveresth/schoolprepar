{% extends 'admin/base.html.twig' %}

{% block title %}Supprimer {{ etablissement.nom ?? 'Établissement' }} - SchoolPrepar{% endblock %}

{% block page_header %}
    <h1 class=\"h3 mb-3\"><strong>Supprimer Établissement</strong></h1>
{% endblock %}

{% block body %}
<div class=\"row justify-content-center\">
    <div class=\"col-12 col-lg-8\">
        <div class=\"card border-danger\">
            <div class=\"card-header bg-danger text-white\">
                <h5 class=\"card-title mb-0\">Confirmer suppression</h5>
            </div>
            <div class=\"card-body text-center py-5\">
                <i class=\"fas fa-exclamation-triangle fa-3x text-danger mb-4\"></i>
                <h4>Êtes-vous sûr de vouloir supprimer l'établissement ?</h4>
                <p class=\"text-muted mb-4\">{{ etablissement.nom ?? '' }} ne pourra pas être récupéré.</p>
                
                <form method=\"post\" action=\"{{ path('admin_etablissement_delete', {'id': etablissement.id ?? ''}) }}\" style=\"display: inline;\">
                    <input type=\"hidden\" name=\"_method\" value=\"DELETE\">
                    <input type=\"hidden\" name=\"_token\" value=\"{{ csrf_token('delete_etablissement') }}\">
                    
                    <div class=\"d-flex justify-content-center gap-3\">
                        <a href=\"{{ path('admin_etablissement') }}\" class=\"btn btn-secondary\">Annuler</a>
                        <button type=\"submit\" class=\"btn btn-danger\" onclick=\"return confirm('Confirmer la suppression?')\">Supprimer</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>
{% endblock %}
