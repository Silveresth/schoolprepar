{% extends 'front/base.html.twig' %}
{% block title %}SchoolPrepar - Orientez votre avenir{% endblock %}

{% block body %}

{# ===== HERO ===== #}
<section class="mk-hero">
    <div class="container">
        <div class="row align-items-center">
            <div class="col-lg-7 mk-hero-content">
                <span class="mk-badge-pill mb-3 d-inline-block" style="background:rgba(255,255,255,.2);color:#fff;">Plateforme d'orientation scolaire</span>
                <h1>Trouvez la filière<br>qui vous correspond</h1>
                <p>SchoolPrepar vous aide à explorer les filières et établissements disponibles au Togo pour faire le meilleur choix pour votre avenir.</p>
                <div class="d-flex flex-wrap gap-2">
                    <a href="{{ path('filiere') }}" class="mk-btn-primary">
                        <i class="fas fa-graduation-cap"></i> Explorer les filières
                    </a>
                    <a href="{{ path('etablissement') }}" class="mk-btn-outline">
                        <i class="fas fa-school"></i> Voir les établissements
                    </a>
                </div>
            </div>
        </div>
    </div>
</section>

{# ===== STATS CARDS ===== #}
<section class="mk-section" style="background:#fff;">
    <div class="container">
        <div class="row g-4 justify-content-center" style="margin-top:-60px;">
            <div class="col-sm-6 col-lg-3">
                <div class="mk-card card p-4 text-center h-100">
                    <div class="mk-card-icon bg-gradient-primary mx-auto">
                        <i class="fas fa-book-open"></i>
                    </div>
                    <h3 class="fw-bold mb-1" style="color:#344767;">{{ stats.total_filieres ?? 0 }}</h3>
                    <p class="text-muted mb-0 small">Filières disponibles</p>
                </div>
            </div>
            <div class="col-sm-6 col-lg-3">
                <div class="mk-card card p-4 text-center h-100">
                    <div class="mk-card-icon bg-gradient-success mx-auto">
                        <i class="fas fa-university"></i>
                    </div>
                    <h3 class="fw-bold mb-1" style="color:#344767;">{{ stats.total_etablissements ?? 0 }}</h3>
                    <p class="text-muted mb-0 small">Établissements partenaires</p>
                </div>
            </div>
            <div class="col-sm-6 col-lg-3">
                <div class="mk-card card p-4 text-center h-100">
                    <div class="mk-card-icon bg-gradient-warning mx-auto">
                        <i class="fas fa-users"></i>
                    </div>
                    <h3 class="fw-bold mb-1" style="color:#344767;">{{ stats.total_utilisateurs ?? 0 }}</h3>
                    <p class="text-muted mb-0 small">Utilisateurs inscrits</p>
                </div>
            </div>
            <div class="col-sm-6 col-lg-3">
                <div class="mk-card card p-4 text-center h-100">
                    <div class="mk-card-icon bg-gradient-dark mx-auto">
                        <i class="fas fa-calendar-alt"></i>
                    </div>
                    <h3 class="fw-bold mb-1" style="color:#344767;">{{ stats.total_evenements ?? 0 }}</h3>
                    <p class="text-muted mb-0 small">Événements planifiés</p>
                </div>
            </div>
        </div>
    </div>
</section>

{# ===== DERNIERES FILIERES ===== #}
<section class="mk-section" style="background:#f8f9fa;">
    <div class="container">
        <div class="text-center mb-5">
            <p class="text-uppercase fw-bold small" style="color:#1A73E8;letter-spacing:2px;">Découvrir</p>
            <h2 class="mk-section-title">Dernières filières ajoutées</h2>
            <p class="mk-section-subtitle">Explorez les formations disponibles dans les établissements du Togo</p>
        </div>
        <div class="row g-4">
            {% for filiere in dernieres_filieres %}
            <div class="col-md-6 col-lg-4">
                <div class="mk-card card h-100">
                    <div class="card-body p-4">
                        <div class="d-flex align-items-center gap-3 mb-3">
                            <div class="mk-card-icon bg-gradient-primary mb-0" style="width:44px;height:44px;flex-shrink:0;">
                                <i class="fas fa-book" style="font-size:1rem;"></i>
                            </div>
                            <div>
                                <h6 class="fw-bold mb-0" style="color:#344767;">{{ filiere.nom }}</h6>
                                <small class="text-muted">{{ filiere.etablissement.nom }}</small>
                            </div>
                        </div>
                        <p class="text-muted small mb-3" style="line-height:1.6;">
                            {{ filiere.description|length > 100 ? filiere.description|slice(0,100) ~ '...' : filiere.description }}
                        </p>
                        <div class="d-flex justify-content-between align-items-center">
                            <span class="mk-badge-pill">{{ filiere.niveauRequis }}</span>
                        </div>
                    </div>
                </div>
            </div>
            {% else %}
            <div class="col-12 text-center py-4 text-muted">
                Aucune filière pour le moment.
                <a href="{{ path('admin_filiere_new') }}">En ajouter ?</a>
            </div>
            {% endfor %}
        </div>
        <div class="text-center mt-5">
            <a href="{{ path('filiere') }}" class="mk-btn-primary">
                <i class="fas fa-arrow-right"></i> Voir toutes les filières
            </a>
        </div>
    </div>
</section>

{# ===== CTA ===== #}
<section class="mk-section" style="background: linear-gradient(195deg,#42424a,#191919);">
    <div class="container text-center">
        <div style="color:#fff;">
            <h2 class="fw-bold mb-3" style="font-size:2rem;">Prêt à construire votre avenir ?</h2>
            <p style="opacity:.8;font-size:1.1rem;margin-bottom:2rem;">Explorez nos établissements partenaires et trouvez le cursus idéal pour vous.</p>
            <a href="{{ path('etablissement') }}" class="mk-btn-primary">
                <i class="fas fa-school"></i> Voir les établissements
            </a>
        </div>
    </div>
</section>

{% endblock %}
